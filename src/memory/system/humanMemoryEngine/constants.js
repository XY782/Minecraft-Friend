const DEFAULT_DECAYS = [1.0, 0.9, 0.75, 0.5, 0.25]
const DEFAULT_CONSOLIDATION_MS = 120_000
const DEFAULT_WORKING_WINDOW_MS = 180_000

module.exports = {
  DEFAULT_DECAYS,
  DEFAULT_CONSOLIDATION_MS,
  DEFAULT_WORKING_WINDOW_MS,
}
